医学生临床问诊模拟实训与智能评估系统 —— 1.0 Demo 规划
📌 项目定位
为医学生提供一个可交互的、具备真实临床逻辑的虚拟患者问诊环境，并在问诊结束后，由 AI 导师给出多维度的量化评估报告。

🛠 一、 功能模块设计（Demo 核心）
为了让小 Demo 快速跑通且具备完整闭环，我们聚焦于以下三个核心模块：

1. 智能虚拟患者模块 (AI Patient)
角色扮演与状态维持： LLM 扮演具有特定主诉、现病史、既往史和隐瞒信息的患者。

情绪与语气模拟： 根据设定的性格（如焦虑、配合、啰嗦），在对话中展现不同的情绪。

信息漏斗机制： 遵循“不问不答，问对才答”的原则。例如，学生如果不主动询问“是否有药物过敏”，AI 绝不主动交代。

2. 问诊工作台模块 (Workspace)
流式对话： 沉浸式聊天框，支持文本输入（进阶可考虑语音输入），AI 类似 ChatGPT 逐字生成回复。

结构化病历实时记录： 右侧提供标准 SOAP 病历（主诉、现病史等）或“病史采集单”填写区域，训练医学生边问边记的习惯。

“结束问诊并生成报告”按钮： 触发评估流程。

3. 智能评估与反馈模块 (Evaluation)
多维度量化评分： 对比标准病例的“知识点得分”，评估病史采集完整度、临床思维逻辑、医患沟通技巧等。

可视化报告： 使用 ECharts 生成雷达图。

AI 导师点评： 逐条指出漏问的黄金问题，并给出改进建议。

💻 二、 推荐技术栈
1. 前端（追求极致的开发体验与流畅度）
核心框架： React 18 + TypeScript（类型安全在处理复杂表单时很有帮助）。

构建工具： Vite（热更新极快，秒级启动）。

UI 组件库： Ant Design (Antd)（国内后台系统天花板，组件极其丰富，适合快速搭建工作台）。

图表库： ECharts 或 Recharts（用于绘制评估雷达图）。

2. 后端（追求高并发与 AI 原生支持）
核心框架： FastAPI (Python)。完美支持异步（async/await），原生支持 SSE（Server-Sent Events），非常适合流式输出。

大模型编排： LangChain。用于管理患者的 Prompt、短期对话记忆（Window Buffer Memory），以及评估阶段的链式调用（Chains）。

LLM API： 建议使用 DeepSeek-V3 / R1（性价比极高且中文逻辑极强）或 GPT-4o-mini 作为基座。

3. 数据库
关系型数据库： SQLite（Demo 阶段首选，无需安装，一个文件搞定）或 PostgreSQL（若后续要部署）。存储病例模版、学生问诊记录。

🚀 三、 分步骤实施指南（如何从零开发）
第一阶段：后端最小可行性产品 (MVP) 跑通
环境初始化： 创建 Python 虚拟环境，安装 fastapi, uvicorn, langchain, langchain-openai。

设计病例 Prompt：

在后端硬编码一个病例（例如：急性阑尾炎）。

Prompt 需要包含：“你现在是一位 25 岁的男性腹痛患者。你的主诉是...，如果医生没有问你是否有转移性右下腹痛，你绝对不能主动说出来。请用通俗、偶尔焦虑的语气回答。”

实现流式接口： 使用 FastAPI 的 StreamingResponse，配合 LangChain 的 .stream() 方法，让接口能流式吐出患者的回答。

第二阶段：前端界面搭建与对接
初始化项目： 使用 npm create vite@latest 创建 React TS 项目。

搭建工作台布局：

利用 Antd 的 Layout，左边放一个带滚动条的 List 作为对话记录，底部放 Input 和发送按钮。

右边放一个由 Form 组成的卡片，供医学生填写初步诊断和病史采集要点。

实现流式接收： 使用前端的 fetch API 结合 ReadableStream，接收后端的流式数据，并实时更新 React 状态，实现“蹦字”效果。

第三阶段：智能评估逻辑开发（项目的亮眼点）
评估 Prompt 编写：

当用户点击“结束问诊”时，将整段对话上下文和学生填写的病历一起打包发给另一个 LLM（评估导师）。

Prompt 设计：“请作为资深医学导师。根据以下标准病例和学生的问诊对话，为学生打分（满分 100）。请严格按照 JSON 格式返回：{ scores: { 沟通: 80, 逻辑: 70... }, suggestions: [...] }”。

前端可视化： 前端拿到 JSON 数据后，解析出分数，喂给 ECharts 渲染出漂亮的雷达图。
